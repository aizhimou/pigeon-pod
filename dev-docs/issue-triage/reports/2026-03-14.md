# Daily Issues Report - 2026-03-14

## Cursor Snapshot
- report_date: 2026-03-14
- last_incremental_sync: null
- selected_issues: [124, 121, 120, 119, 118, 111, 105, 103, 100, 86, 70]
- carry_over_issues: []

## Agent Report
### #124 [FR] Add a way to go back to the system yt-dlp
- Class: requirement
- Source summary: User switched to a managed nightly/master yt-dlp build to work around YouTube changes, then had to delete `/data/tools/yt-dlp/versions` and `/data/tools/yt-dlp/current` manually because the UI only lets them choose update channels, not revert to the system binary.
- Relevant touchpoints:
  - `backend/src/main/java/top/asimov/pigeon/service/YtDlpRuntimeService.java:82-95`
  - `backend/src/main/java/top/asimov/pigeon/service/YtDlpRuntimeService.java:148-161`
  - `backend/src/main/java/top/asimov/pigeon/model/response/YtDlpRuntimeInfoResponse.java:12-20`
  - `frontend/src/pages/Setting/index.jsx:1658-1677`
- Analysis: The backend already falls back to `SYSTEM_BINARY` when no managed `current` runtime is usable, but the API does not expose execution mode and there is no explicit action to clear the managed `current` link. The current settings modal only exposes `stable` and `nightly`, so the user-facing workflow is incomplete even though the runtime resolver already supports the fallback path.
- Recommendation: approve_task
- Proposed next action: Add an explicit "use system yt-dlp" action and surface the active runtime mode in the settings response/UI.
- Proposed task title: Add explicit fallback to system yt-dlp
- Proposed task description: Extend the yt-dlp runtime API and settings UI so users can switch from managed runtimes back to the system binary without manual file deletion. The backend should clear or ignore the managed `current` link safely, preserve installed managed versions for later reuse, and return the active runtime mode so the UI can display whether PigeonPod is using a managed module or the system binary.
- Suggested fields:
  - Priority: Medium
  - Size: S
  - Estimate: 1-2 days
- Draft reply: Thanks for the report. The fallback behavior already exists internally, but the settings UI does not currently expose a safe way to switch back to the system yt-dlp binary. I recommend turning this into a small settings/API task so users do not need to delete files under `/data` manually.
- Open questions: Should "use system yt-dlp" keep the downloaded managed versions on disk for later reuse, or should it optionally prune them?

### #121 RSS feed description
- Class: requirement
- Source summary: User wants the `Source video:` link moved from the beginning of the RSS item description to the end so podcast apps show the episode summary text first.
- Relevant touchpoints:
  - `backend/src/main/java/top/asimov/pigeon/service/RssService.java:155-159`
  - `backend/src/main/java/top/asimov/pigeon/service/RssService.java:205-245`
- Analysis: The RSS generator currently prepends the source-video prefix before the episode description for every item. This is a low-risk formatting change with clear user value because many podcast clients truncate from the start of the description. The only real product choice is whether playlist source-channel attribution should also move or remain as a header line.
- Recommendation: approve_task
- Proposed next action: Move the source-video line to the end of the generated summary while preserving HTML escaping and current playlist/channel attribution behavior.
- Proposed task title: Move source link to end of RSS descriptions
- Proposed task description: Update `RssService.buildEpisodeSummary` so the episode description body appears first and the `Source video` link is appended after it. Keep existing escaping, line-break behavior, and playlist source-channel attribution intact unless there is a deliberate UX decision to move both metadata blocks together.
- Suggested fields:
  - Priority: Medium
  - Size: S
  - Estimate: <1 day
- Draft reply: This looks like a straightforward RSS formatting improvement. The current implementation always prepends the source-video link, so moving it to the end should improve how item previews look in podcast clients without changing feed structure.
- Open questions: Should `Source channel:` for playlist items stay at the top, or should both attribution lines move together to the end?

### #120 Enable chapters
- Class: discussion
- Source summary: User does not see a chapter toggle and expected audio-only Podcasting 2.0 chapters in Pocket Casts; they also observed custom yt-dlp args being overridden.
- Relevant touchpoints:
  - `backend/src/main/java/top/asimov/pigeon/handler/DownloadHandler.java:399-405`
  - `backend/src/main/java/top/asimov/pigeon/handler/DownloadHandler.java:551-555`
  - `backend/src/main/java/top/asimov/pigeon/handler/DownloadHandler.java:735-787`
  - `backend/src/main/java/top/asimov/pigeon/handler/DownloadHandler.java:813-865`
  - `backend/src/main/java/top/asimov/pigeon/service/RssService.java:196-199`
  - `backend/src/main/java/top/asimov/pigeon/service/RssService.java:361-377`
- Analysis: PigeonPod already auto-generates Podcasting 2.0 `podcast:chapters` metadata when yt-dlp provides chapter data in `info.json`, and it does this without a dedicated UI toggle. The app also forces `--write-info-json` after custom args, so this is not a missing checkbox so much as either source-data absence or podcast-client support/interpretation mismatch. The right next step is a clarifying reply plus a concrete sample feed/episode if the user believes chapter data exists but is not showing up.
- Recommendation: reply_and_monitor
- Proposed next action: Reply with current behavior, clarify there is no manual chapter toggle today, and ask for one concrete episode/feed example so chapter-file generation can be verified.
- Proposed task title:
- Proposed task description:
- Suggested fields:
  - Priority:
  - Size:
  - Estimate:
- Draft reply: PigeonPod does not currently expose a manual "enable chapters" toggle because chapter export is automatic when yt-dlp returns chapter data. For audio feeds, the app writes Podcasting 2.0 chapter metadata into the RSS only if a chapters file can be generated from the downloaded episode metadata. If you can share one feed plus one episode where you expect chapters, that would make it possible to verify whether this is missing source chapter data or a Pocket Casts client limitation.
- Open questions: Does the affected source video actually contain chapter markers, and does the generated episode expose `/media/{episodeId}/chapters.json`?

### #118 Feature Request: Auto-Retry for Failed Downloads + Feed Error Notifications
- Class: requirement
- Source summary: User wants failed downloads retried automatically with configurable interval and max attempts, plus a visible notification path when retries are exhausted.
- Relevant touchpoints:
  - `backend/src/main/java/top/asimov/pigeon/scheduler/DownloadScheduler.java:37-84`
  - `backend/src/main/java/top/asimov/pigeon/handler/DownloadHandler.java:190-205`
  - `backend/src/main/java/top/asimov/pigeon/service/EpisodeService.java:660-709`
- Analysis: The first half already exists in a hard-coded form: the scheduler re-picks `FAILED` episodes with `retry_number < 3` whenever slots are available, and failures increment `retry_number`. What is missing is policy control, spacing/backoff, and an operator-facing notification channel once retries are exhausted. This is a good fit for the current scheduler/state-machine architecture, but it should be split into an MVP and a later notification design rather than bundling RSS pollution into the first delivery.
- Recommendation: approve_task
- Proposed next action: Create a scoped task for configurable failed-download retry policy first; keep RSS/error-notification delivery as a follow-up design decision.
- Proposed task title: Make failed download retries configurable
- Proposed task description: Replace the current implicit failed-download retry policy (`retry_number < 3` with immediate scheduler re-pickup) with configurable settings for max attempts and retry delay/backoff. Keep the existing `Episode` state flow and scheduler model, and defer user-facing exhausted-retry notifications to a second task after the retry policy is explicit and testable.
- Suggested fields:
  - Priority: Medium
  - Size: M
  - Estimate: 2-4 days
- Draft reply: There is already a basic automatic retry path for failed downloads, but it is currently hard-coded and not exposed as a policy users can control. I recommend turning this into a scheduler/settings task first, then deciding separately whether exhausted retries should surface via email, dashboard alerts, or another channel rather than writing synthetic RSS items into the feed immediately.
- Open questions: Should retry policy live in global defaults only, or also be overrideable per feed?

### #119 Force hw accelaration by default?
- Class: discussion
- Source summary: User on Raspberry Pi 4 is trying to force FFmpeg hardware acceleration through custom yt-dlp postprocessor args, asks whether those args are correct, whether the container supports it, and whether PigeonPod should enable this by default.
- Relevant touchpoints:
  - `backend/src/main/java/top/asimov/pigeon/util/YtDlpArgsValidator.java:14-27`
  - `backend/src/main/java/top/asimov/pigeon/handler/DownloadHandler.java:499-545`
  - `Dockerfile:16-22`
- Analysis: This is mostly an environment/support discussion, not a narrowly scoped product task. PigeonPod passes validated custom yt-dlp args through to yt-dlp and FFmpeg, but it does not detect host GPU capabilities, configure device passthrough, or tune per-platform hardware acceleration defaults. The runtime image includes FFmpeg, but container-level hardware acceleration on Raspberry Pi still depends on host device exposure, FFmpeg build capabilities, and a codec path that is stable for the user's workload. Making this "default" globally would be risky because the correct flags are platform-specific and can regress ordinary installs.
- Recommendation: reply_only
- Proposed next action: Reply with product-boundary clarification: custom FFmpeg args may work, but hardware acceleration is host/container dependent and should not be forced by default without a reproducible supported matrix.
- Proposed task title:
- Proposed task description:
- Suggested fields:
  - Priority:
  - Size:
  - Estimate:
- Draft reply: PigeonPod currently just forwards allowed yt-dlp/FFmpeg arguments; it does not manage hardware-acceleration device setup inside the container. Whether Raspberry Pi hardware acceleration works depends on Docker device mapping, the FFmpeg build, and the specific codec path, so this is not something PigeonPod can safely enable by default for all users. If you want to keep digging, the next useful step would be a concrete container run configuration plus FFmpeg capability output from your Pi environment.
- Open questions: Is the container being started with the necessary video device mappings, and does the packaged FFmpeg build on that host expose the required Raspberry Pi acceleration path?

### #111 Option to add youtube video id to the filename
- Class: discussion
- Source summary: User initially asked for a filename format containing the YouTube video ID for Plex metadata-agent compatibility, but the thread shifted into external library organization behavior and a maintainer explanation about product boundaries.
- Relevant touchpoints:
  - `backend/src/main/java/top/asimov/pigeon/util/YtDlpArgsValidator.java:14-27`
  - `backend/src/main/java/top/asimov/pigeon/handler/DownloadHandler.java:506-509`
  - `backend/src/main/java/top/asimov/pigeon/util/MediaKeyUtil.java:21-28`
- Analysis: The request is intentionally blocked today because custom `-o/--output` naming is disallowed, and the download pipeline assumes a stable `safeTitle`-based local filename plus a separate S3 key strategy. The issue thread has also already converged on a product-boundary discussion: PigeonPod is not trying to optimize naming conventions for every external media manager. Given the maintainer's 2026-03-11 reply and the user's exploratory follow-up, this looks closer to a boundary clarification than an urgent implementation task.
- Recommendation: close_candidate
- Proposed next action: Treat the current maintainer response as sufficient unless more users request a generic filename-template feature that is broader than Plex-specific integration.
- Proposed task title:
- Proposed task description:
- Suggested fields:
  - Priority:
  - Size:
  - Estimate:
- Draft reply: None. The current thread already explains why filename customization is blocked today and why PigeonPod is cautious about optimizing file naming for one external media manager. Unless the request broadens into a general filename-template feature with wider value, this looks ready to close.
- Open questions: Is there any non-Plex use case that would justify a generic filename template feature instead of a one-off integration setting?

### #105 Downloads stuck in pending/downloading
- Class: bug
- Source summary: Reporter and at least one other user saw downloads stall until container restart. On 2026-03-11 the maintainer replied that v1.24.0 includes a fix for stalls under heavy concurrent subscription updates and asked the reporter to upgrade.
- Relevant touchpoints:
  - `backend/src/main/java/top/asimov/pigeon/config/AsyncConfig.java:16-35`
  - `backend/src/main/java/top/asimov/pigeon/helper/DownloadTaskHelper.java:16-69`
  - `backend/src/main/java/top/asimov/pigeon/scheduler/DownloadScheduler.java:37-84`
- Analysis: This thread already has a maintainer response pointing to a shipped mitigation in v1.24.0, and the current download pipeline is centered on a fixed-size executor, explicit slot control, and scheduler-based backfill. There is no new evidence in the issue after the upgrade request, so opening another task now would be premature. The next useful signal is whether the reporter can still reproduce the problem on the post-fix version with fresh logs.
- Recommendation: need_info
- Proposed next action: Wait for user validation on v1.24.0+. If the issue still reproduces, collect new logs and environment details under the upgraded build before re-opening implementation work.
- Proposed task title:
- Proposed task description:
- Suggested fields:
  - Priority:
  - Size:
  - Estimate:
- Draft reply: None. The maintainer already asked for validation on the v1.24.0 fix on 2026-03-11, so the next action should be reporter confirmation rather than another immediate code task.
- Open questions: Has the reporter upgraded to v1.24.0 or later, and if so do stalls still occur with fresh logs from that version?

### #103 [FR} Rumble support?
- Class: requirement
- Source summary: User wants PigeonPod to support Rumble sources similarly to YouTube because some creators they follow are no longer on YouTube.
- Relevant touchpoints:
  - `README.md:28-33`
  - `backend/src/main/java/top/asimov/pigeon/service/FeedService.java:135-165`
  - `backend/src/main/java/top/asimov/pigeon/helper/YoutubeHelper.java:48-78`
  - `backend/src/main/java/top/asimov/pigeon/helper/BilibiliResolverHelper.java:22-57`
  - `backend/src/main/java/top/asimov/pigeon/service/YtDlpPlaylistSnapshotService.java:56-74`
- Analysis: This is a real product requirement, but it is not a small extractor toggle. The current ingestion architecture is explicitly source-aware and only has concrete source handling for YouTube and Bilibili across source detection, metadata fetch, channel/playlist resolution, history sync, and RSS/source-link generation. Even though yt-dlp may download individual Rumble media URLs, PigeonPod would still need a source model, discovery/fetch path, sync strategy, and RSS/source-link behavior for Rumble feeds before the experience is product-complete.
- Recommendation: defer
- Proposed next action: Keep this as a roadmap requirement, but do not schedule it as a small feature. It needs explicit source-abstraction scope and a realistic discovery/sync design first.
- Proposed task title: Investigate Rumble source support architecture
- Proposed task description: Evaluate what would be required to add Rumble as a first-class feed source in PigeonPod, including source detection, channel/playlist discovery, incremental sync without YouTube APIs, metadata normalization, RSS/source-link generation, and fallback behavior if only yt-dlp extraction is available.
- Suggested fields:
  - Priority: Low
  - Size: XL
  - Estimate: multi-release
- Draft reply: Rumble support is not blocked only on the downloader. PigeonPod’s current subscription and sync architecture is built around YouTube and Bilibili source handling, so adding Rumble would require a broader source-integration design instead of just enabling another extractor. This is a reasonable roadmap item, but it should stay deferred until there is time to design it properly.
- Open questions: Can Rumble provide enough stable channel/feed discovery data for incremental sync, or would the product have to accept a reduced feature set compared with YouTube/Bilibili?

### #100 [FR]Cache RSS content as a file and store it in S3
- Class: requirement
- Source summary: Internal requirement to cache generated RSS as files in S3 so PigeonPod can stay private/local while clients fetch both feed and media from object storage.
- Relevant touchpoints:
  - `dev-docs/architecture/architecture-design-zh.md:19-20`
  - `backend/src/main/java/top/asimov/pigeon/service/RssService.java:120-159`
  - `backend/src/main/java/top/asimov/pigeon/service/MediaService.java:314-370`
  - `backend/src/main/java/top/asimov/pigeon/service/AccountService.java:701-705`
- Analysis: This aligns with the current S3 direction, but the implementation gap is meaningful. Media, subtitles, and chapter files already get S3-aware URLs, yet RSS itself is still served dynamically from `/api/rss/**` and requires the app base URL and API key flow. To make the app fully non-public, PigeonPod would need a publish/cache lifecycle for feed XML, a storage key strategy, invalidation on episode/feed updates, and a decision about whether the RSS object is public, presigned, or regenerated per subscriber credential model.
- Recommendation: approve_task
- Proposed next action: Create a design-first task for static RSS publication to storage, scoped separately from the existing media-object flow.
- Proposed task title: Publish cached RSS to S3
- Proposed task description: Add a storage-backed RSS publication path so channel/playlist feeds can be rendered to XML files and uploaded to S3, with update triggers on episode/feed mutations. The design should cover storage keys, refresh timing, invalidation, access model, and how this interacts with current API-key-protected dynamic RSS endpoints.
- Suggested fields:
  - Priority: Medium
  - Size: L
  - Estimate: 4-7 days
- Draft reply: This fits the existing storage direction, but it is more than a small toggle because RSS is currently generated dynamically while media assets already have S3-aware delivery. I recommend treating it as a separate storage-publication task with explicit design around feed publishing and access control.
- Open questions: Should cached RSS be public objects, signed objects, or a parallel optional export path separate from the existing dynamic RSS endpoints?

### #86 VPS freezes due to OOM when adding playlist (Ubuntu 24.04, 1GB RAM)
- Class: bug
- Source summary: User on a 1 GB RAM VPS reports reproducible OOM when adding YouTube playlists with 2+ videos; channel plus single-video cases remain stable. The issue persisted after v1.21.0 according to user feedback.
- Relevant touchpoints:
  - `backend/src/main/java/top/asimov/pigeon/service/YtDlpPlaylistSnapshotService.java:51-120`
  - `backend/src/main/java/top/asimov/pigeon/service/PlaylistService.java:374-454`
  - `backend/src/main/java/top/asimov/pigeon/service/PlaylistService.java:594-694`
  - `dev-docs/architecture/architecture-design-zh.md:71-74`
- Analysis: This is a credible product bug. The current playlist path is much more memory-sensitive than simple channel sync because it builds a yt-dlp snapshot, remote/local mapping structures, and added-entry batches before persisting results. There have already been optimizations in this area, but the thread has no confirmation on a newer build beyond the older v1.21.0 test. The right move is not to close it; it still needs either fresh validation on the current playlist pipeline or a bounded repro using the latest release.
- Recommendation: need_info
- Proposed next action: Ask for a retest on the latest version and collect fresh `docker stats`, container logs, and playlist size details if the issue still reproduces.
- Proposed task title:
- Proposed task description:
- Suggested fields:
  - Priority:
  - Size:
  - Estimate:
- Draft reply: The issue still looks valid, but the playlist sync path has changed since the original report, so the next high-signal step is a retest on the latest release with fresh memory data. If it still OOMs, the issue should stay open and move back into implementation with a reproducible latest-version profile.
- Open questions: Does the current latest release still OOM on the same playlist and machine profile, and approximately how large is the affected playlist?

### #70 Can you do a few videos of setting up the application?
- Class: discussion
- Source summary: User asked for setup tutorial videos, then the thread moved into an off-platform collaboration offer where the user volunteered to help edit videos and said they would continue on Reddit.
- Relevant touchpoints:
  - `README.md:56-124`
  - `README.md:163-205`
- Analysis: This is no longer an implementation issue. It is a community/content discussion that already moved off the issue tracker, and the repository already has written setup documentation covering Docker and local development. There is no pending product decision or engineering task here unless the maintainer explicitly wants to track tutorial-production work in GitHub.
- Recommendation: close_candidate
- Proposed next action: Close the issue as completed/offline follow-up unless the maintainer wants to convert it into a docs/content project item.
- Proposed task title:
- Proposed task description:
- Suggested fields:
  - Priority:
  - Size:
  - Estimate:
- Draft reply: None. The discussion already moved to an external coordination path, and there is no remaining repo work item in the issue itself.
- Open questions: Does the maintainer want tutorial production tracked as a project task, or should this stay outside GitHub issues?

## Developer Reply
### #124
Decision: 添加任务添加到 project 中，等待开发
Priority Override:
Size Override:
Estimate Override:
Task Title Override:
Notes: 提供一个下拉框将当前所有可用的 yt-dlp 版本列出来，供用户在必要时自行选择

### #121
Decision: 回复用户这是一个很好的建议，感谢他的建议。然后把这个任务添加到 project，等待开发
Priority Override:
Size Override:
Estimate Override:
Task Title Override:
Notes: channel 和 playlist 类型的订阅，「source video」内容都移动到 show note 结尾

### #120
Decision: reply_only
Priority Override:
Size Override:
Estimate Override:
Task Title Override:
Notes: 按照你的建议回复用户，并且告知用户不同的 podcast client 对章节信息的支持程度不一样，而且也并非每一个 YouTube 节目本身会包含章节信息，对于本身包含了章节信息的 YouTube 节目，PigeonPod 会按照 podcasting 2.0 协议的标准生成章节信息，请用户检查他订阅的 YouTube 节目本身是否包含章节信息，然后查看 RSS 输出中是否有 `podcast:chapters` 标签，如果有 `podcast:chapters` 标签而不展示章节信息，那就是他所使用的 podcast client 并不支持标准的 podcasting 2.0 协议，可以更换一个客户端再试试。

### #119
Decision: reply_only
Priority Override:
Size Override:
Estimate Override:
Task Title Override:
Notes: 按照你的建议回复用户，并且告知用户我很难针对每一个用户特定的硬件环境提供建议，因为我的手上不可能有这么多类型的硬件，建议他可以尝试和 AI 讨论针对他的硬件环境的硬件加速问题。

### #118
Decision: 添加任务到 project 中，等待开发
Priority Override: Medium
Size Override: L
Estimate Override: 1 day
Task Title Override:
Notes:

### #111
Decision: 产品边界问题，已经给了用户对应的建议，等待用户回复。
Priority Override:
Size Override:
Estimate Override:
Task Title Override:
Notes:

### #105
Decision: 我已经手动回复，等到用户进一步回复
Priority Override:
Size Override:
Estimate Override:
Task Title Override:
Notes:

### #103
Decision: 添加任务到 project 中，等待开发
Priority Override: Medium
Size Override: XL
Estimate Override: 2 - 3 days
Task Title Override:
Notes:

### #100
Decision: 添加任务到 project 中，等待开发
Priority Override: Low
Size Override: L
Estimate Override: 1 day
Task Title Override:
Notes:

### #86
Decision: 添加任务到 project 中，等待处理
Priority Override: Low
Size Override: L
Estimate Override: 1 day
Task Title Override: Reduce PigeonPod's memory footprint when running on machines with limited memory(less than 1GB)
Notes:

### #70
Decision: 关闭这个 issue，并用英文回复：经过几个月的发展，PigeonPod 的功能迭代很快，而视频教程的制作成本和修改成本太高，一个视频教程做完可能很快就会更不上最新版本的 UI 和功能。所以我决定建设更加详细和系统的 Wiki 内容来帮助用户更好的使用和理解 PigeonPod。文字内容的制作成本和修改成本更低，易于翻译，内容更容易被检索，且更容易与 Ai 交互和协作。感谢你提出帮 PigeonPod 制作视频教程，如果你愿意的话，可以参与我们后续的 Wiki 内容的建设。
Priority Override: Low
Size Override:
Estimate Override:
Task Title Override:
Notes:

## Execution Receipt
- executor_run_at: 2026-03-14T01:52:03Z
- project_items_created:
  - Add yt-dlp runtime selector with system fallback
  - Move source attribution to end of show notes
  - Make failed download retries configurable
  - Investigate Rumble source support architecture
  - Publish cached RSS to S3
  - Reduce PigeonPod's memory footprint when running on machines with limited memory(less than 1GB)
- issues_added_to_project: []
- issue_replies_posted:
  - #121 https://github.com/aizhimou/pigeon-pod/issues/121#issuecomment-4059131849
  - #120 https://github.com/aizhimou/pigeon-pod/issues/120#issuecomment-4059131848
  - #119 https://github.com/aizhimou/pigeon-pod/issues/119#issuecomment-4059131850
  - #70 closed with comment
- cursor_updates:
  - #124 moved to terminal `done_synced` after project draft creation
  - #121 moved to terminal `done_synced` after reply + project draft creation
  - #120 moved to active `monitoring_discussion` after maintainer-approved reply
  - #119 moved to active `monitoring_discussion` after maintainer-approved reply
  - #118 moved to terminal `done_synced` after project draft creation
  - #111 left active as `monitoring_discussion` because maintainer noted no new action
  - #105 left active as `need_more_info` because maintainer already replied manually
  - #103 moved to terminal `done_synced` after project draft creation
  - #100 moved to terminal `done_synced` after project draft creation
  - #86 left active as `approved_for_task`; task synced to project and still needs implementation follow-through
  - #70 moved to terminal `closed_no_action` after close + comment
